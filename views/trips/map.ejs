<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title><%- title %></title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
  <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.10.0/font/bootstrap-icons.css">
  <link rel="stylesheet" href="https://unpkg.com/leaflet@1.9.4/dist/leaflet.css" />
  <link rel="stylesheet" href="/css/style.css">
  <!-- Core Application CSS -->
  <link rel="stylesheet" href="/css/core.css">
  <style>
    #map {
      height: 700px;
      width: 100%;
      border-radius: 0.375rem;
      box-shadow: 0 0.125rem 0.25rem rgba(0,0,0,0.075);
    }
  </style>
</head>
<body>
  <main class="container-fluid mt-4">
    <%- include('../partials/flash') %>
    
    <div class="d-flex justify-content-between align-items-center mb-3">
      <h1><%- trip.name %> - Map View</h1>
      <a href="/trips/<%- trip.id %>" class="btn btn-secondary">
        <i class="bi bi-arrow-left"></i> Back to Trip
      </a>
    </div>

    <div id="map"></div>
    
    <div class="mt-3 mb-3">
      <h5>Legend</h5>
      <span class="badge bg-primary me-2"><i class="bi bi-airplane"></i> Flight</span>
      <span class="badge bg-success me-2"><i class="bi bi-building"></i> Hotel</span>
      <span class="badge" style="background-color: #fd7e14;"><i class="bi bi-train-front"></i> Transportation</span>
      <span class="badge bg-secondary me-2 ms-2"><i class="bi bi-car-front"></i> Car Rental</span>
      <span class="badge bg-danger me-2"><i class="bi bi-calendar-event"></i> Event</span>
    </div>
    
    <div class="alert alert-info">
      <i class="bi bi-info-circle"></i> 
      <strong>Note:</strong> Locations are geocoded using OpenStreetMap. The blue dashed line shows your route in chronological order. 
      Click markers for details. Geocoding may take a few moments.
    </div>
  </main>

  <%- include('../partials/footer') %>

  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
  <script src="https://unpkg.com/leaflet@1.9.4/dist/leaflet.js"></script>
  <script src="/js/datetime-formatter.js?v=<%= assetVersion %>"></script>
  <script src="/js/maps.js?v=<%= assetVersion %>"></script>
  <script>
    // Prepare trip data for the map
    const tripData = {
      flights: <%- JSON.stringify(trip.flights) %>,
      hotels: <%- JSON.stringify(trip.hotels) %>,
      transportation: <%- JSON.stringify(trip.transportation) %>,
      carRentals: <%- JSON.stringify(trip.carRentals) %>,
      events: <%- JSON.stringify(trip.events) %>
    };
    
    // Initialize map when page loads
    document.addEventListener('DOMContentLoaded', function() {
      initializeMap(tripData);
    });
  </script>
</body>
</html>
